name: Deprecated Staging Workflow

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Stop deprecated AWS staging workflow
        run: |
          echo "This workflow is intentionally disabled."
          echo "FundEU is operating on GCP, not AWS/ECS."
          echo "Define a real GCP staging service before re-enabling automated staging deploys."
          exit 1
