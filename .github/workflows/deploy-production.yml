name: Production Deploy Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment path retired
        run: |
          echo "GitHub Actions is no longer a production deployment path for FundEU."
          echo "Use Google Cloud Build instead."
          echo "Canonical config: cloudbuild.production.yaml"
          echo "Canonical docs: docs/CLOUD-BUILD-CD.md"
