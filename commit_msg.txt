feat(ai): complete Task 4 & 5 migration + AI client upgrade

Finishes the initial AI migration sprint by migrating grant matching
and compliance validation to the multi-provider system.

Changes:
- Implemented matchGrants in new grant-matcher module with Romanian
  specialization and budget-tier routing.
- Migrated validateCompliance to use the upgraded AI client and
  task-based routing (classification).
- Upgraded core ai-client to return enhanced metadata (provider, model,
  cached status) and support task-specific routing tiers.
- Fixed type errors across the project caused by the client upgrade
  (chat, compliance-engine, deadline-intelligence, knowledge-extractor,
  risk-assessment, RAG pipeline).
- Added unit tests for grant matching and compliance validation.

Co-Authored-By: Gemini CLI <gemini-cli@google.com>
