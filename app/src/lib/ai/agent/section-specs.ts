// Re-export from shared orchestrator section specs to avoid duplication.
// Both agent and orchestrator use identical section definitions.
export { DEFAULT_SECTIONS, buildSectionSpecs, compactPreviousSections } from '@/lib/ai/orchestrator/section-specs'
